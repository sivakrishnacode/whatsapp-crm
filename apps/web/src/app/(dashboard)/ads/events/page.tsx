import { AdsEvents } from '@/components/ads/ads-events';

/** Ads Manager → Events. The Meta Pixel and the conversion events website ads optimise for. */
export default function AdsEventsPage() {
  return <AdsEvents />;
}
