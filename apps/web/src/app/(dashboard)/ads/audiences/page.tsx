import { AdsAudiences } from '@/components/ads/ads-audiences';

/** Ads Manager → Audiences. Custom audiences from CRM segments, plus lookalikes. */
export default function AdsAudiencesPage() {
  return <AdsAudiences />;
}
